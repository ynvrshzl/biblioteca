When are abstractions justified? I believe a rule of thumb is: abstraction is justified, when the work to 

cost redundancy of ... would otherwise exceed it's abstraction.

This also presents a stable definition of when abstraction is not justified; when it over-complicates.