{
  "name": "@lib.dataviewjs",
  "version": "1.0.0",
  "description": "Obsidian DataviewJS Library and template engine",
  "main": "main.js",
  "scripts": {
    "watch": "esbuild src/lib.js --bundle --minify --platform=browser --format=iife --global-name=metatpl --outfile=compile.js --log-level=verbose --watch",
    "compile": "esbuild src/lib.js --bundle --minify --platform=browser --format=iife --global-name=metatpl --outfile=compile.js --log-level=verbose --watch",
    "start": "npm run watch",
	"build": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "hazl@192.168.1.75:~/iostream/remotes/local.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
