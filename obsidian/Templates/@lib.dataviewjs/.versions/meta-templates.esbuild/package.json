{
  "name": "local",
  "version": "1.0.0",
  "description": "Metalib",
  "main": "main.js",
  "scripts": {
    "watch": "esbuild src/lib.js --bundle --minify --platform=browser --format=iife --global-name=metatpl --outfile=runtime.js --log-level=verbose --watch",
    "start": "npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "hazl@192.168.1.75:~/iostream/remotes/local.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
