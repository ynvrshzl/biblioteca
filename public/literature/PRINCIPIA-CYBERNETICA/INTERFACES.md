In summary, a library or package can be thought of as a node in a network, i.e., an API in a larger program. It's an "interface" becuase it exposes visibility, ideally, to only the essential details, i.e., input, output, and implementations. The realization of those abstract ideas is formally hidden within an interface of abstractions.

