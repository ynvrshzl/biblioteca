# Interface
###### Author, Date, Properties, Etc.
A static architecture for dynamic information

Read more about Transformation [here.](Public/E100-KOSMOKON/10-KYBERNAUTICS/Transformation.md)

In Kybernautics, the "Interface" is the irreducible, "atomos"  micro-model of Transformation. The interface is a static structure of **Input, Process and Output; Transformation.** For the **Dynamic transformation and Processing of Information.**

The interface is a fascinating model as Transformation is the language of all Dynamic living things and Computation.

$$
\sum o = x(i)
$$

The transformation is derived as the distance between the unknown or dynamic "o" output and the known or dynamic "i" input:

$$
\sum x = d(o, i)
$$

The distance between "i" input and "o" output is derived by direction; forth or backtraced. IN other words, are we reverse engineering a structure to understand it's internal components? Dissassembly/Decomposition ..or are we building forward, or Compsing Abstractions?

When direction is established, an object of objective and action/cause emerges

$$
\sum o = f(Obj. + Act.)
$$