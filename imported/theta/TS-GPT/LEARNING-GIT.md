  now that I have understood the fundamentals of git, I no longer rely on any graphical interface or application, and I also want to understand the technology for its expensive vocabulary, it's set of features!

so far I understand...

- git add . (stages all changes to the repository file system. including deletions, modifications, creations, renames. basic CRUD operations but at the snapshot level.)
- git commit -m "<message>" or git commit (opens nano, which is another interesting micro utility living in the command line, similar to VI. essentially, git commit is exactly as it sounds, changes are moved from the buffer stage, to permanent diskwrite to the repository fs. ) 